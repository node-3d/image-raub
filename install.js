'use strict';

const install = require('addon-tools-raub/install');


const prefix = 'https://github.com/node-3d/image-raub/releases/download';
const tag    = 'v3.2.0-pre';

install(`${prefix}/${tag}`);
